public String nameFromEmail(String email) {
    //code here
    return email.substring(0,email.indexOf('.')).tolowercase().capitalize()+' '+
    email.substring(email.indexOf('.')+1,email.indexOf('@')).tolowercase().capitalize();
}